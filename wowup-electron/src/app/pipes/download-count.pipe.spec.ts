import { DownloadCountPipe } from './download-count.pipe';

describe('DownloadCountPipe', () => {
  it('create an instance', () => {
    const pipe = new DownloadCountPipe();
    expect(pipe).toBeTruthy();
  });
});
