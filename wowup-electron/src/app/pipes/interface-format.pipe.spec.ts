import { InterfaceFormatPipe } from './interface-format.pipe';

describe('InterfaceFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new InterfaceFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
